import QtQuick 2.15
import QtQuick.Controls 2.15
import "."

Rectangle {
    anchors.fill: parent
    color: Style.primaryColor

    // Library button
    // Horizontal seperator
    // List of playlists
    ListView {
    }
}